using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HideToggleSwitch : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // hide the object until we detect a lamp in our environment
        gameObject.SetActive(false);
    }

    // Update is called once per frame
    void Update()
    {
        // if user has clicked on toggle, turn the lamp on or off
        


    }
}
